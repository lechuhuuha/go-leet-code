// using sliding window + hash map
// init the l,r of the window with both at position 0
// init the hash map with string as key and value as int to represent the last seen of that char
assume i talking in string not in runes
runes should be the same but is a number
and i dont remember number represent that string

left = 0
result = 0

first loop
right = 0
char := a
is not in the seenMap
so we add it to the seenMap
with key as the char so we know next time the char comeup
and the value is the position of that char which is right current index
we compute the length with
length := (right - left) + 1 = 1
right now length > result
so result = length

next loop
right = 1
char = b
is not in the seenMap
so we add it to the seenMap
with key as the char so we know next time the char comeup
and the value is the position of that char which is right current index
we compute the length with
length := (right - left) + 1 = (1 - 0) + 1 = 2
right now length > result
so result = length

next loop
right = 2
char = c
is not in the seenMap
so we add it to the seenMap
with key as the char so we know next time the char comeup
and the value is the position of that char which is right current index
we compute the length with
length := (right - left) + 1 = (2 - 0) + 1 = 3
right now length > result
so result = length

next loop
right = 3
char = a
is in the seenMap
we move the left window to the position of that char + 1
+ 1 is for move pass that char to check the new sub string
now left is 1 which is b

so we add it to the seen map (in this case is update)
seenMap[a] = 3
which is current right index and the second a position
we compute the length with
length := (right - left) + 1 = (3 - 1) + 1 = 3
right now length = result
nothing

next loop
right = 4
char = b
is in the seenMap
we move the left window to the position of that char + 1
+ 1 is for move pass that char to check the new sub string
now left is 2 which is c
so we add it to the seen map (in this case is update)
seenMap[b] = 4
which is current right index and the second b position
we compute the length with
length := (right - left) + 1 = (4 - 2) + 1 = 3
right now length = result
nothing

next loop
right = 5
char = c
is in the seenMap
we move the left window to the position of that char + 1
+ 1 is for move pass that char to check the new sub string
now left is 3 which is a
so we add it to the seen map (in this case is update)
seenMap[c] = 5
which is current right index and the second c position
we compute the length with
length := (right - left) + 1 = (5 - 3) + 1 = 3
right now length = result
nothing

next loop
right = 6
char = b
we continue until right = 8
and left now is at 8 since the last seen b is at 7

there is no result larger then 3
we return 3

QA
Complexity
Time is O(n) since we walk through every element in slice
Space is O(n) we still turn the s into runes
 n(input) n (temp array) = 2n = n

Why do we need this condition?

lastSeen >= left

Give one example where removing this condition would make the answer wrong.

we need this because left is represent for left window 
and the window can not have duplicate characters in it 
if we allow left to smaller then the last seen
that mean the substring will have repeated characters
which is why we need >=
so we can move the left window skip the repeated char in last seen
that what + 1 is for